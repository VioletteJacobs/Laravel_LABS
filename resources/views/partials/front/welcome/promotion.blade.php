	<!-- Promotion section -->
	<div class="promotion-section">
		<div class="container">
			<div class="row">
				<div class="col-md-9">
					<h2>{{$ready[0]->title}}</h2>
					<p>{{$ready[0]->p}}</p>
				</div>
				<div class="col-md-3">
					<div class="promo-btn-area">
						<a href="{{$ready[0]->btn_href}}" class="site-btn btn-2">{{$ready[0]->btn_content}}</a>
					</div>
				</div>
			</div>
		</div>
	</div>
	<!-- Promotion section end-->